import { styled } from '@nextui-org/react';

export const StyledSubscribeInput = styled('input', {
  display: 'inline',
  color: 'darkblue',
  p: '0 20px',
  border: 'none',
  height: '41px',
  borderRadius: '0',
});
