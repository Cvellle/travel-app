import { styled, Container } from '@nextui-org/react';

export const StyledSubscribeContainer = styled(Container, {
  p: '100px 0',
  color: 'white',
  backgroundColor: 'darkblue',
  justifyContent: 'center',
  alignItems: 'center',
  w: '100%',
});
