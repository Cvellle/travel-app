import { styled } from '@nextui-org/react';

export const StyledIconWrapper = styled('div', {
  display: 'flex',
  flexWrap: 'wrap',
  alignItems: 'center',
  gap: '15px',
  marginBottom: '30px',
});
