import { styled } from '@nextui-org/react';

export const PrintButton = styled('div', {
  w: '3rem',
  h: '3rem',
  backgroundImage: `url('images/icons/print.png')`,
  backgroundSize: 'cover',
  backgroundPosition: 'center',
  border: 0,
  cursor: 'pointer',
});
